import 'package:flutter/material.dart';

class HoverBoard extends StatelessWidget {
  const HoverBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
